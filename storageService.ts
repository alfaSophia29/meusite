// Fix: Corrigir collection name de 'logs' para 'system_logs'

const storageService = {
    collectionName: 'system_logs', // Atualizado de 'logs' para 'system_logs'
    // outros métodos e propriedades
};

export default storageService;